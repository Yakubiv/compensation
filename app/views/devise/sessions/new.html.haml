.container
  .row
    .col-sm-6.offset-sm-3
      .login-form.padder-h
        %h2.text-center.padder-h-sm Увійдіть у систему
        = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          = devise_error_messages!
          %p.padder-v
            = f.text_field :username, placeholder: 'Логін', autofocus: true, class: 'form-control input-lg'
          %p
            = f.password_field :password, placeholder: 'Пароль', class: 'form-control input-lg'
          - if devise_mapping.rememberable?
            .field
              = f.check_box :remember_me
              = f.label :remember_me, 'Запам’ятати мене'
          %p.padder-v
            = f.submit 'Увійти', class: 'btn btn-lg btn-block'
